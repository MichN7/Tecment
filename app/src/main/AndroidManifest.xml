<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.michellereyes.tecment">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TestActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            LoginActivity
        </activity>
        <activity android:name=".LevelsActivity" />
        <activity android:name=".Level1Activity" />
        <activity android:name=".Level2Activity" />
        <activity android:name=".Level3Activity" />
        <activity android:name=".Level4Activity" />
        <activity android:name=".TotalActivity" />
        <activity android:name=".TotalLevel2Activity" />
        <activity android:name=".TotalLevel3Activity" />
        <activity android:name=".TotalLevel4Activity"></activity>
    </application>

</manifest>